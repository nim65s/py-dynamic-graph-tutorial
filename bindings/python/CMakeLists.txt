ADD_LIBRARY(${PY_NAME} SHARED ${PY_NAME})
PKG_CONFIG_USE_DEPENDENCY(${PY_NAME} "py-dynamic-graph")
TARGET_LINK_LIBRARIES(${PY_NAME} ${PROJECT_NAME} "py_dynamic_graph${PYTHON_SOABI}")
TARGET_LINK_BOOST_PYTHON(${PY_NAME})
INSTALL(TARGETS ${PY_NAME} DESTINATION ${${PY_NAME}_INSTALL_DIR})
